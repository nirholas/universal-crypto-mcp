apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-notify.fullname" . }}
  labels:
    {{- include "mcp-notify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-notify.selectorLabels" . | nindent 4 }}
