// Package models contains database model types.
// Models are defined in pkg/types for shared use across packages.
// This package can be extended with database-specific model variations if needed.

package models

// Re-export types from the main types package for database operations.
// See pkg/types/types.go for the canonical type definitions.
