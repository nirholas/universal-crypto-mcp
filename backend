#!/bin/bash
#===============================================================================
# Backend CLI - Quick access to backend automation
#===============================================================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_SCRIPTS="$SCRIPT_DIR/scripts/backend"

# Forward all commands to the main automation script
exec "$BACKEND_SCRIPTS/backend-automation.sh" "$@"
